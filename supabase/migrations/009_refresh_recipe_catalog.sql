-- ============================================================
-- Recipe catalog refresh from updated Vista Component Recipie New.xlsx
-- Generated by scripts/generate_recipe_refresh_sql.py
-- Workbook snapshot: 2026-05-07
-- Note: current workbook contains 8 sections, with one duplicate
-- Roller with Headrail section merged into one canonical blind recipe.
-- ============================================================

BEGIN;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('Roller Blinds Without Headrail', 'Roller Blinds Without Headrail', 'Imported from Vista Component Recipie New.xlsx', true, 'Roller W/o Headrail Parts') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'aluminium roller tube (4575) 38 mm' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALUMINIUM ROLLER TUBE (4.575) 38 MM'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu bottom rail (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU BOTTOM RAIL (4.575 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'control unit ks 38 mm' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CONTROL UNIT KS 38 MM'), 1.0, false, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Double Sided Fixon Tape 12MM'), 4.0, false, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'cord weight' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CORD WEIGHT'), 1.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'connector - bush type' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Connector - Bush Type'''), 1.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'bottom rail end cap' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Bottom Rail End Cap'''), 2.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'operating ball chain' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Operating Ball Chain'''), 4.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 15mm' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 15MM'), 2.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 12 MM'), 2.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'chain stopper' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Chain Stopper'), 1.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL SCREW 8X50'), 7.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL PLUG 8X50'), 7.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds Without Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'poly tube 5' ORDER BY CASE WHEN c.normalized_name = 'roller w/o headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'POLY TUBE 5"'), 2.0, false, 14 FROM r JOIN v ON true;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('Roller Blinds With Headrail', 'Roller Blinds With Headrail', 'Imported from Vista Component Recipie New.xlsx', true, 'Roller with Headrail Parts') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu roller tube 39mm (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU ROLLER TUBE 39MM (4.575 MTR)'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu round bottom rail(457 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. ROUND BOTTOM RAIL(4.57 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'aluheadrail patti (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU.HEADRAIL PATTI (4.575 MTR)'), 1, true, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'control unit 38mm' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CONTROL UNIT 38MM'), 1.0, false, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Double Sided Fixon Tape 12MM'), 4.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'cord weight' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CORD WEIGHT'), 1.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'connector - bush type' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Connector - Bush Type'''), 1.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'bottom rail end cap' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Bottom Rail End Cap'''), 2.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'operating ball chain' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Operating Ball Chain'''), 4.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 15mm' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 15MM'), 2.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 12 MM'), 2.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'chain stopper' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Chain Stopper'), 1.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'l bracket' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'L BRACKET.'), 4.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'p clip' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'P. CLIP'), 4.0, false, 14 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL PLUG 8X50'), 7.0, false, 15 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL SCREW 8X50'), 7.0, false, 16 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Headrail' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'poly tube 5' ORDER BY CASE WHEN c.normalized_name = 'roller with headrail parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'POLY TUBE 5"'), 2.0, false, 17 FROM r JOIN v ON true;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('Sheer Dimout Blinds Classic Mechanism with Plain Cassette', 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette', 'Imported from Vista Component Recipie New.xlsx', true, 'Sheer Dimout Plain Cassette Parts') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu roller tube 39mm (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU ROLLER TUBE 39MM (4.575 MTR)'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu round bottom rail(457 mtr)' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. ROUND BOTTOM RAIL(4.57 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu triangle type bottom rail' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. TRIANGLE TYPE BOTTOM RAIL'), 1, true, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu headrail with cover' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Alu. Headrail with Cover'), 1, true, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'control unit ks 38 mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CONTROL UNIT KS 38 MM'), 1.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'end less chain' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'End Less Chain'), 1.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'end cap round bottom rail' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'END CAP ROUND BOTTOM RAIL'), 2.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'end cap triangular bottom rail' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'END CAP TRIANGULAR BOTTOM RAIL'), 2.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'metal end cap' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Metal End Cap'), 1.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'ceilling bracket' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CEILLING BRACKET'''), 3.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'cord weight' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CORD WEIGHT'), 1.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL SCREW 8X50'), 7.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL PLUG 8X50'), 7.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 10mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'P.V.C INSERT 10MM'), 2.0, false, 14 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 12 MM'), 2.0, false, 15 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Double Sided Fixon Tape 12MM'), 4.0, false, 16 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'poly tube 5' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'POLY TUBE 5"'), 2.0, false, 17 FROM r JOIN v ON true;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('Sheer Dimout Blinds Classic Mechanism with Decorative Cassette', 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette', 'Imported from Vista Component Recipie New.xlsx', true, 'Sheer Dimout Decorative Cassette Parts') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu roller tube 39mm (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU ROLLER TUBE 39MM (4.575 MTR)'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu round bottom rail(457 mtr)' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. ROUND BOTTOM RAIL(4.57 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu u type bottom rail' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. U TYPE BOTTOM RAIL'), 1, true, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'control unit' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CONTROL UNIT.'), 1.0, false, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'end less chain' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'End Less Chain'), 1.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'end cap round bottom rail' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'END CAP ROUND BOTTOM RAIL'), 2.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'end cap u type bottom rail' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'END CAP U TYPE BOTTOM RAIL'), 2.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'metal end cap' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Metal End Cap'), 1.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'ceilling bracket' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CEILLING BRACKET'''), 3.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'cord weight' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CORD WEIGHT'), 1.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 10mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'P.V.C INSERT 10MM'), 2.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 12 MM'), 2.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Double Sided Fixon Tape 12MM'), 4.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'poly tube 5' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'POLY TUBE 5"'), 2.0, false, 14 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu headrail with cover' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Alu. Headrail with Cover'), 2.0, false, 15 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'chain stopper' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Chain Stopper'), 1.0, false, 16 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc sheet width 87 mm' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC SHEET WIDTH 87 MM'), 2.0, false, 17 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL SCREW 8X50'), 7.0, false, 18 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Sheer Dimout Blinds Classic Mechanism with Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL PLUG 8X50'), 7.0, false, 19 FROM r JOIN v ON true;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('S-Contour Blinds', 'S-Contour Blinds', 'Imported from Vista Component Recipie New.xlsx', true, 'S Contour Parts') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-alu roller tube 39mm (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-ALU ROLLER TUBE 39MM (4.575 MTR)'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-alu bottom rail (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-ALU BOTTOM RAIL (4.575 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-alu headrail with cover' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-Alu. Headrail with Cover'), 1, true, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-control unit 38mm' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-CONTROL UNIT 38MM'), 1.0, false, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-end less chain' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-End Less Chain'), 1.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-bottom rail end cap' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-Bottom Rail End Cap'''), 2.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-chain stopper' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-Chain Stopper'), 1.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-metal end cap' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-Metal End Cap'), 1.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-pvc sheet width 87 mm' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-PVC SHEET WIDTH 87 MM'), 2.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-ceiling bracket' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-CEILING BRACKET'), 3.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-cord weight' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-CORD WEIGHT'), 1.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-ROYAL SCREW 8X50'), 7.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-ROYAL PLUG 8X50'), 7.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-PVC INSERT 12 MM'), 2.0, false, 14 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-Double Sided Fixon Tape 12MM'), 4.0, false, 15 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'S-Contour Blinds' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'contour-poly tube 5' ORDER BY CASE WHEN c.normalized_name = 's contour parts' THEN 0 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Contour-POLY TUBE 5"'), 2.0, false, 16 FROM r JOIN v ON true;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('Roller Blinds With Plain Cassette', 'Roller Blinds With Plain Cassette', 'Imported from Vista Component Recipie New.xlsx', true, 'Roller with Plain Cassette') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu roller tube 39mm (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU ROLLER TUBE 39MM (4.575 MTR)'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu round bottom rail(457 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. ROUND BOTTOM RAIL(4.57 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'aluheadrail patti (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU.HEADRAIL PATTI (4.575 MTR)'), 1, true, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu headrail with cover' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Alu. Headrail with Cover'), 1, true, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'control unit 38mm' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CONTROL UNIT 38MM'), 1.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Double Sided Fixon Tape 12MM'), 4.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'cord weight' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CORD WEIGHT'), 1.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'connector - bush type' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Connector - Bush Type'''), 1.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'bottom rail end cap' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Bottom Rail End Cap'''), 2.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'operating ball chain' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Operating Ball Chain'''), 4.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 15mm' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 15MM'), 2.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 12 MM'), 2.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'chain stopper' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Chain Stopper'), 1.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'l bracket' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'L BRACKET.'), 4.0, false, 14 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'p clip' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'P. CLIP'), 4.0, false, 15 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL PLUG 8X50'), 7.0, false, 16 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL SCREW 8X50'), 7.0, false, 17 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Plain Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'poly tube 5' ORDER BY CASE WHEN c.normalized_name = 'roller with plain cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'POLY TUBE 5"'), 2.0, false, 18 FROM r JOIN v ON true;

INSERT INTO public.product_recipes (name, blind_type, description, is_active, notes) VALUES ('Roller Blinds With Decorative Cassette', 'Roller Blinds With Decorative Cassette', 'Imported from Vista Component Recipie New.xlsx', true, 'Roller with Decorative Cassette') ON CONFLICT DO NOTHING;
DELETE FROM public.recipe_items WHERE recipe_id IN (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette');
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu roller tube 39mm (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU ROLLER TUBE 39MM (4.575 MTR)'), 1, true, 1 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu round bottom rail(457 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU. ROUND BOTTOM RAIL(4.57 MTR)'), 1, true, 2 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'aluheadrail patti (4575 mtr)' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ALU.HEADRAIL PATTI (4.575 MTR)'), 1, true, 3 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'alu headrail with cover' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Alu. Headrail with Cover'), 1, true, 4 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'control unit 38mm' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CONTROL UNIT 38MM'), 1.0, false, 5 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'double sided fixon tape 12mm' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Double Sided Fixon Tape 12MM'), 4.0, false, 6 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'cord weight' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'CORD WEIGHT'), 1.0, false, 7 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'connector - bush type' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Connector - Bush Type'''), 1.0, false, 8 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'bottom rail end cap' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Bottom Rail End Cap'''), 2.0, false, 9 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'operating ball chain' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Operating Ball Chain'''), 4.0, false, 10 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 15mm' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 15MM'), 2.0, false, 11 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'pvc insert 12 mm' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'PVC INSERT 12 MM'), 2.0, false, 12 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'chain stopper' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'Chain Stopper'), 1.0, false, 13 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'l bracket' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'L BRACKET.'), 4.0, false, 14 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'p clip' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'P. CLIP'), 4.0, false, 15 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal plug 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL PLUG 8X50'), 7.0, false, 16 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'royal screw 8x50' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'ROYAL SCREW 8X50'), 7.0, false, 17 FROM r JOIN v ON true;
WITH r AS (SELECT id FROM public.product_recipes WHERE blind_type = 'Roller Blinds With Decorative Cassette' LIMIT 1), v AS (SELECT v.id, v.unit, v.name FROM public.inv_variants v JOIN public.inv_products p ON p.id = v.product_id JOIN public.inv_categories c ON c.id = p.category_id WHERE v.normalized_name = 'poly tube 5' ORDER BY CASE WHEN c.normalized_name = 'roller with decorative cassette' THEN 0 WHEN c.normalized_name = 'roller with headrail parts' THEN 1 WHEN c.normalized_name = 'sheer dimout plain cassette parts' THEN 2 WHEN c.normalized_name = 'sheer dimout decorative cassette parts' THEN 3 ELSE 999 END, c.name, p.name, v.name LIMIT 1) INSERT INTO public.recipe_items (recipe_id, variant_id, component_name, quantity_per_unit, is_width_dependent, sort_order) SELECT r.id, v.id, COALESCE(v.name, 'POLY TUBE 5"'), 2.0, false, 18 FROM r JOIN v ON true;

COMMIT;

SELECT
  (SELECT COUNT(*) FROM public.product_recipes) AS product_recipes,
  (SELECT COUNT(*) FROM public.recipe_items) AS recipe_items,
  (SELECT COUNT(*) FROM public.recipe_items WHERE variant_id IS NULL) AS unmatched_recipe_items;
